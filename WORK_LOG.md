# WORK_LOG

## 2026-06-01 ワールドマップSVGパス地理精度向上

### 作業概要
`_renderWorldMap()`の大陸SVGパスを、実際の緯度経度座標から算出した地理的に正確なパスに全面置換。

### 実施内容
- 全30パス（大陸・島）を実際のlat/lon座標から`x=(lon+180)*1000/360, y=(90-lat)*500/180`で計算
- 北米: ハドソン湾のくぼみ、フロリダ半島、メキシコ湾、バハカリフォルニア、アラスカ半島を明確化
- 南米: ブラジル東部の突出、パタゴニアの先細り、アンデス沿いの直線的西海岸
- ヨーロッパ: イベリア半島、バルカン半島、スカンジナビア、英国・アイルランドを個別パスで表現
- アフリカ: ギニア湾のくぼみ、アフリカの角、喜望峰、マダガスカル
- アジア: トルコ〜カムチャッカ〜中国沿岸〜インド亜大陸〜中東を1パスで描画、アラビア半島は別パス
- 日本: 本州・北海道・九州を個別パス、種子島ピン位置と整合
- 東南アジア: ボルネオ、スマトラ、ジャワ、スラウェシ、ニューギニアを個別島パス
- オーストラリア: カーペンタリア湾、グレートオーストラリアンバイトの凹み、ケープヨーク
- 全パスZ閉じ、最大1113文字（2000文字制限内）

### 変更ファイル
- `js/screens.js` — `_renderWorldMap()`内の`continents`配列パス文字列のみ置換

---

## 2026-06-01 射点画面整理＋旋回レート修正

### 作業概要
射点選択画面からロケットイラストを削除、Lv2/3で墜落しやすい旋回レートを緩和

### 実施内容
1. **ロケットイラスト削除** (screens.js `renderLaunch`)
   - `.launch-rocket-preview`セクションを削除、射点倍率/精度表示も簡略化
2. **旋回レート緩和** (physics.js)
   - `maxPitchRateDeg`: 1.5→0.8(低空)、3.0→2.0(上層)、5.0→3.5(真空)
   - `tangentC`: `(1.2 + alt/500000) * (1 + tw*0.3)` → `(1.0 + alt/600000) * (1 + tw*0.2)` に緩和
   - より穏やかな重力ターンでLv2/3（高緯度で自転ブーストが小さい）での墜落を防止
3. **キャッシュバスト** v=20 → v=21

### 変更ファイル
- `js/screens.js` — ロケットイラスト削除
- `js/physics.js` — maxPitchRateDeg, tangentC緩和
- `index.html` — v=21

---

## 2026-06-01 ワールドマップ大幅リニューアル

### 作業概要
打ち上げ画面のワールドマップを全面的に刷新。地図の拡大、横スクロール対応、大陸アウトラインの詳細化を実施。

### 実施内容
- マップコンテナを70vh高さに変更、横スクロール対応（overflow-x: auto）
- SVGのviewBoxを360x180から1000x500に拡大
- SVG幅を250%に設定し、水平スクロールで全地域を閲覧可能に
- 全大陸のSVGパスを実際の緯度経度座標から正距円筒図法で生成した詳細パスに置換
  - 北米（アラスカ〜中米）、グリーンランド、南米、ヨーロッパ、スカンジナビア、英国・アイルランド
  - イタリア、シチリア、アフリカ、マダガスカル
  - アジア大陸（ロシア〜中国〜東南アジア）、アラビア半島、インド亜大陸、スリランカ
  - 朝鮮半島、日本列島（本州・北海道・九州）、台湾、フィリピン
  - ボルネオ、スマトラ、ジャワ、スラウェシ、ニューギニア、マレー半島
  - オーストラリア、タスマニア、ニュージーランド、アイスランド、南極帯
- ピン座標計算を新viewBoxスケールに対応（lonToX/latToY関数）
- ピンサイズ（半径、フォントサイズ）を新スケールに合わせて拡大
- 緯度・経度のグリッドライン追加（経度線30度間隔）
- スクロールバーのスタイリング追加
- pinPulseアニメーションのstroke-widthを新スケールに調整

### 変更ファイル
- `js/screens.js` — `_renderWorldMap()`メソッド全面書き換え
- `css/style.css` — `.world-map-container`、`.world-map-svg`、ピン関連CSS更新

---

## 2026-06-01 オーバーラップ検証修正＋ドラッグオフセット修正

### 作業概要
1. structure/engineが重なった状態で設計完了が承認されるバグを修正
2. パーツのドラッグ&ドロップ先が1マス右にズレるバグを修正

### 実施内容
1. **オーバーラップ配置制限** (garage.js `_canPlace`)
   - `allowOverlap`を`fairing`のみに限定（`structure`はオーバーラップ不可に）
2. **オーバーラップバリデーション追加** (garage.js `_validate`)
   - fairing+payload/obc以外の重なりを検出し「部品が重なっています」エラーを返す
3. **接続検出のオーバーラップ制限** (garage.js `_computeConnections`)
   - オーバーラップ接続をfairing+payload/obcペアのみに限定
4. **ドラッグオフセット修正** (garage.js `_screenToWorkspace`)
   - `parentElement.getBoundingClientRect()`→`_gridEl.getBoundingClientRect()`に変更
   - flexbox `justify-content: center`によるオフセットを正しく考慮

### 変更ファイル
- `js/garage.js` — _canPlace, _validate, _computeConnections, _screenToWorkspace

---

## 2026-06-01 射点選択UIをインタラクティブ世界地図に置換

### 作業概要
打ち上げ画面の射点選択をボタンリストからSVGワールドマップに変更。メルカトル風の簡易大陸アウトラインにピンを配置し、クリックで射点を選択できるようにした。

### 実施内容
1. **`_renderWorldMap()`メソッド追加** (js/screens.js)
   - 簡易大陸パス（7大陸）を含むインラインSVG生成
   - 各射点をピンとして配置（経度を分散配置して重複回避）
   - アクティブピンは赤(primary)でハイライト＋パルスアニメーション
   - ロック済みピンは暗色＋ロックアイコン表示
   - 選択時に地図下部にポップアップカード表示（射点名、倍率、精度、機能）
2. **CSSスタイル追加** (css/style.css)
   - `.world-map-container`, `.world-map-svg` レスポンシブ地図コンテナ
   - `.map-pin`, `.pin-dot`, `.pin-ring`, `.pin-label` ピンスタイル
   - `.map-site-popup` 射点詳細ポップアップ
   - `@keyframes pinPulse` アクティブピンのアニメーション
3. **キャッシュバージョン更新** (index.html) v=19 → v=20

### 変更ファイル
- `js/screens.js` — `renderLaunch()`内の射点リストを地図に置換、`_renderWorldMap()`追加
- `css/style.css` — ワールドマップ関連スタイル追加
- `index.html` — キャッシュバージョンv=20

## 2026-05-31 ガレージ接続検証バグ修正（Phase 10）

### 作業概要
「部品が結合されていません」エラーが、全パーツ配置済みでも発生するバグを修正。原因はグリッド上の段間ギャップとBFS接続判定の厳密さ。

### 実施内容
1. **`_compactLayout()`メソッド追加** (garage.js)
   - 設計完了ボタン押下時、バリデーション前にグリッド上の垂直ギャップを自動閉鎖
   - 同一列・同一幅のパーツをrow順にソートし、隣接パーツ間のギャップを詰める
   - 変更があれば`_saveLayout()` → `_render()` → `_unbindEvents()` → `_bindEvents()` → `_updateStats()`

2. **`_computeConnections()`更新** (garage.js)
   - エッジ隣接（`pB.row === bottomRowA`）に加え、オーバーラップ検出を追加
   - structure/fairingが他パーツと同じセルを共有する場合も接続として認識
   - `seen` Setで重複ペア防止

3. **`_onComplete()`更新** (garage.js)
   - `_compactLayout()` → `_computeConnections()` → `_validate()` の順で実行
   - バリデーション通過時「承認されました」トースト表示

4. **イベント再バインディング修正** (garage.js)
   - `_compactLayout()`内で`_render()`後にDOM要素が再生成されるため、`_unbindEvents()` → `_bindEvents()`を追加
   - 設計完了ボタン等のクリックイベントが再レンダリング後も動作するように

### 変更ファイル
- `js/garage.js` — _compactLayout, _computeConnections, _onComplete, イベント再バインディング
- `index.html` — キャッシュバスト v=18 → v=19

---

## 2026-05-31 打ち上げアニメーションSVGコンポジット化（Phase 9）

### 作業概要
打ち上げアニメーションのロケット描画を、Canvas手描きシェイプから射点プレビューと同一のSVGイラスト（コンポジット画像）に置き換え。

### 実施内容
1. **`_buildRocketComposites()`メソッド追加** (app.js)
   - ステージ数×フェアリング有無の全組み合わせでコンポジットSVG画像を事前生成
   - キー形式: `stg{N}_fair{0|1}`（例: `stg0_fair1`, `stg1_fair0`）
   - `G.Garage._svgTank/Fairing/Structure/Engine/OBC/Payload`を使用（プレビューと同一関数）
   - パーツの流し込み配置: フェアリング(g.h分のみ前進)、段間構造(margin-top:-24px相当)
   - Z-order: payload/obc=-1, tank/engine=0, structure=5, fairing=10
   - ネストSVGでコンポジット構築 → Blob URL → Image変換

2. **Canvas描画の置き換え** (app.js `_playLaunchAnimation`)
   - 旧: フェアリング/段ボディ/段間/ノズルをCanvasで個別描画
   - 新: `ctx.drawImage(_comp.img, ...)` でコンポジット画像1枚を描画
   - アスペクト比保持: `fitW = fitH * comp.width / comp.height`
   - ステージ分離時: `stg${d.stage}_fair{...}` でキー切替（下段除去）
   - フェアリング分離時: `fair0` に切替（フェアリング無し版）

3. **排気炎の維持**
   - Canvas描画のまま維持（リアルタイムランダム揺れが必要なため）
   - ノズル幅をエンジン推力からスケーリング: `pow(thrust/55, 0.25)`

4. **キャッシュバスト更新** (index.html) v16→v17

### テスト結果
- ✅ 射座上のロケット表示（PADフェーズ）
- ✅ 飛行中のロケット表示（回転対応）
- ✅ 排気炎の正常表示
- ✅ コンソールエラーなし

### 変更ファイル一覧
- `js/app.js` — `_buildRocketComposites()`追加、Canvas手描き描画をdrawImage置換、排気炎維持
- `index.html` — キャッシュバスト v16→v17

---

## 2026-05-31 段間構造テーパ対応（Phase 8）

### 作業概要
上段と下段のタンクサイズが異なる場合、段間構造がテーパ（円錐台）形状で両タンクを滑らかに接続するよう変更。ガレージ・射点プレビュー・打ち上げアニメーションの3箇所に反映。

### 実施内容
1. **`_tankBw()`ヘルパー追加** (garage.js)
   - タンクのボディ幅を容量から算出する汎用メソッド: `max(40, min(64, round(52 * pow(cap/2200, 0.2))))`

2. **`_getStructureTankWidths()`追加** (garage.js)
   - 段間構造の上下に隣接するタンクをグリッドから検索
   - 各タンクの`bw`を返し、テーパ描画に使用

3. **`_svgStructure()`テーパ対応** (garage.js)
   - `topBw`/`botBw`パラメータ追加（デフォルト52=同径）
   - 直方体`<rect>`→台形`<path>`に変更
   - 詳細線（パネルライン、ボルト）がテーパに沿って配置
   - 同径の場合は従来通りの直方体表示

4. **ガレージ描画のテーパ対応** (garage.js `_renderGrid`)
   - structure描画時に`_getStructureTankWidths()`で隣接タンク幅を取得
   - `_svgStructure`に`topBw`/`botBw`を渡す

5. **射点プレビューのテーパ対応** (screens.js `_renderRocketPreview`)
   - structure描画時にparts配列から前後のtankを検索
   - `_tankBw()`で幅を算出しSVGに渡す

6. **打ち上げアニメーションのテーパ対応** (app.js `_playLaunchAnimation`)
   - `stageW[]`配列を事前計算（各段の描画幅）
   - 段間描画を`fillRect`→台形`beginPath/moveTo/lineTo`に変更
   - topW=上段幅, botW=下段幅で連続的に接続

7. **キャッシュバスト更新** (index.html) v15→v16

### テーパ例（現在のロケット）
| 位置 | タンク | 容量 | bw |
|------|--------|------|-----|
| 上段 | t1b | 1600 | 49 |
| 下段 | t3a | 4800 | 61 |
→ 段間構造が49→61へテーパ

### 変更ファイル一覧
- `js/garage.js` — `_tankBw`/`_getStructureTankWidths`追加、`_svgStructure`テーパ対応、`_renderGrid`構造体描画分岐追加
- `js/screens.js` — `_renderRocketPreview`で構造体にタンク幅渡し
- `js/app.js` — `stageW[]`事前計算、段間を台形描画
- `index.html` — キャッシュバスト v15→v16

---

## 2026-05-30 物理バグ修正・エンジンバランス・ビジュアルオーバーホール

### 作業概要
T/W<1地表埋没バグ修正、2段/3段用スラスター追加・推力調整、SVGイラストの大幅リアル化（マーリンノズル・SFS風段間・半透過フェアリング・衛星アダプタ）、プレビュー/アニメーションのオーバーレイ表示。

### 実施内容
1. **T/W<1地表埋没バグ修正** (physics.js)
   - `h < 0`時に`h = 0; vr = max(0, vr)`で地上クランプ
   - peakAltitude > 100 && t > 10 で再落下時にシミュレーション終了

2. **エンジンバランス調整** (data.js)
   - e1b: 推力40→30kN(真空)、質量220→135kg、2段目用スラスター化
   - e1c追加: KR-1Mマイクロ 10kN(真空)、45kg、3段目用
   - DEFAULT_PARTS/DEFAULT_INVENTORYにe1c追加

3. **マーリン風ベルノズルSVG** (garage.js)
   - タービンポンプ(2基)・排気管・燃焼室・インジェクタープレート
   - ジンバルリング(楕円)・スロート収縮
   - ベジェ曲線によるパラボリックベルノズル
   - 再生冷却チャネルライン・ノズル内部(暗色)・出口リム

4. **SFS風段間構造SVG** (garage.js)
   - タンクと同色のメタリックグラデーション
   - 半透過(opacity:0.55)の円筒シェル
   - 上下リム・パネルライン・分離ボルト

5. **半透過フェアリングSVG** (garage.js)
   - オジーブ形状 opacity:0.5
   - 内部にペイロードシルエット(衛星本体+太陽電池パネル)
   - シームライン・パネルライン・分離リング

6. **衛星アダプタ(旧OBC)** (data.js, garage.js, screens.js)
   - OBC→SA-x アダプタに名称変更
   - 円錐台ボディ(path)・上下リング・内蔵OBCモジュール(緑基板)
   - LED・分離ボルト・クランプバンド
   - タブラベル: OBC→アダプタ
   - PART_GRID label: 衛星アダプタ

7. **プレビューオーバーレイ表示** (screens.js)
   - フェアリングがペイロードを覆う(margin-bottom:-22px, z-index:10)
   - 段間構造がエンジンノズルを覆う(margin-top:-24px, z-index:5)

8. **キャンバスアニメーション更新** (app.js)
   - ノズル: 台形→ベジェ曲線ベルノズル(内部暗色+出口リム)
   - フェアリング: 半透過(globalAlpha:0.55)+内部ペイロードシルエット
   - 段間構造: 半透過グラデーション円筒+上下リムライン

### 変更ファイル
- `js/data.js` — e1b推力/質量調整、e1c追加、OBC→SAアダプタ名称変更
- `js/physics.js` — 地上クランプ(h<0→h=0)、故障ラベル変更
- `js/garage.js` — _svgEngine(マーリン)、_svgStructure(SFS筒)、_svgFairing(半透過)、_svgOBC(アダプタ)、タブラベル
- `js/screens.js` — プレビューオーバーレイ、ラベル統一(衛星アダプタ)
- `js/app.js` — ベルノズル描画、半透過フェアリング/段間
- `index.html` — キャッシュバスター v=7

---

## 2026-05-29 全画面SVGイラスト統一（ガチャ・打上げ・コレクション）

### 作業概要
ガレージで使用しているSVGイラストを、ガチャ結果・打上げ画面・コレクション画面でも一貫して使用するよう統一。

### 実施内容
- `js/screens.js`: ガチャ結果カードに `.gacha-part-svg` コンテナ + `G.Garage._partSVG()` 呼び出し追加
- `js/screens.js`: コレクションカードに `.col-svg` コンテナ + SVGサムネイル追加（未所持は `.locked-svg` グレースケール）
- `js/screens.js`: `_renderRocketPreview(r)` 追加 — 打上げ画面にロケット部品SVGスタック表示
- `css/style.css`: `.collection-grid` を `display: grid; grid-template-columns: repeat(2, 1fr)` に変更
- `css/style.css`: `.col-svg`, `.gacha-part-svg`, `.rocket-preview-stack`, `.rp-part` スタイル追加

### 変更ファイル
- `js/screens.js` (ガチャSVG、コレクションSVG、ロケットプレビュー追加)
- `css/style.css` (グリッドレイアウト、SVGコンテナスタイル追加)

---

## 2026-05-29 ガレージ部品をリアルSVGイラストに変更

### 作業概要
テキストボックス表示の部品を、形状がわかるリアルなSVGイラストに全面変更。

### 実施内容
- `js/garage.js`: 6カテゴリ分のSVGイラスト生成メソッド追加
  - _svgEngine: 燃焼室+ターボポンプ+スロート+ベルノズル+排気グロー
  - _svgTank: ドーム天板+円筒ボディ+ドーム底板+セグメントバンド+ストリンガー
  - _svgStructure: 上下リング+Xラティストラス+ボルト点
  - _svgFairing: オジーブ形状+スプリットライン+パネルライン
  - _svgPayload: 衛星バス+太陽電池パドル+アンテナ
  - _svgOBC: 回路基板+LED+アンテナ+コネクタポート
- グリッド部品・パレット・タブアイコン・ドラッグゴースト全てSVGに統一
- レアリティカラーをSVGアクセント色として反映
- CSS: テキストベースのスタイルからSVG対応に全面変更

### 変更ファイル
- `js/garage.js` (SVGメソッド追加、レンダリング変更)
- `css/garage.css` (SVG対応スタイル)

---

## 2026-05-29 SFS風ガレージ画面リライト

### 作業概要
旧スロット選択方式のガレージをSFS(Spaceflight Simulator)風のグリッドベース・ドラッグ&ドロップ方式に全面書き換え。

### 実施内容
- `js/garage.js` (新規): グリッドワークスペースモジュール (10×20グリッド, 36pxセル)
  - カテゴリタブ (エンジン/タンク/段間/フェアリング/衛星/OBC)
  - パレットからドラッグ&ドロップで配置
  - タップで部品選択→名前・スペック表示、削除・複製ボタン
  - 隣接部品の自動接続検出 (縦方向のみ、同カラム)
  - BFS接続検証 (全部品が一体か、エンジンにタンク接続あるか)
  - トースト通知 (alert()廃止)
  - ワークスペース→ロケット構成変換 (toRocketConfig)
  - レイアウトのlocalStorage永続化
- `css/garage.css` (新規): グリッド/パーツ/接続/ドラッグゴースト/トースト全スタイル
- `data.js`: G.PART_GRID定数追加、engine.connectBottom=trueに修正
- `index.html`: garage.js, garage.css参照追加
- `screens.js`: renderGarage()をコンテナ返却のみに簡略化
- `state.js`: workspaceLayoutデフォルト追加、getRocketParts()をワークスペース優先に
- `app.js`: navigate('garage')でG.Garage.init/destroy呼び出し

### 検証結果
- グリッド表示・パーツ配置・接続インジケータ表示 ✓
- タブ切替・パレット表示 ✓
- 部品選択→スペック表示・削除/複製ボタン ✓
- 設計完了→承認トースト / 接続エラートースト ✓
- 打ち上げ画面へのロケット構成引き継ぎ ✓

### 変更ファイル
- `js/garage.js` (新規)
- `css/garage.css` (新規)
- `js/data.js` (PART_GRID追加, connectBottom修正)
- `index.html` (スクリプト/CSS参照追加)
- `js/screens.js` (renderGarage簡略化)
- `js/state.js` (workspaceLayout, getRocketParts修正)
- `js/app.js` (navigate garage連携)

---

## 2026-05-29 ロケット地表埋没バグ完全修正（ズーム・データ・遷移）

### 作業概要
ボタン押下前後でロケットのビジュアルが変わり地表に埋没するバグを調査・修正。
初回修正後もジャンプが残存したため、ブラウザでのデバッグ計測を実施し根本原因を特定・完全解消。

### 原因分析（第1弾: 高度ベース遷移）
1. **rYOff = rH2/3 が間違い** → drawH/2に修正
2. **時間ベースのイーズが高度と無関係に開始** → 高度ベース遷移に変更
3. **padScaleがzoomに依存して変動** → 高度ベースで自然解消

### 原因分析（第2弾: デバッグ計測で発見した残存問題）
ブラウザコンソール計測で以下を確認:
- 最終PADフレーム: zoom=1.462, rp.y=391.1, nozzle_y=ground_y=391.1 ✓
- 初回FLIGHTフレーム: zoom=1.389, rp.y=385.7, nozzle_y=387.0, ground_y=388.4 ✗

4. **targetZoomの即時ジャンプ**: LIFTOFF押下でtargetZoom = H/500 → H/3000に即変更
   - 初フレームで zoom = 1.462 + (0.244 - 1.462) * 0.06 = 1.389（5%ズーム変化）
   - 全画面の座標が一斉にシフト → 目に見えるジャンプ
5. **data[0].alt ≈ 2m**: 物理記録間隔2秒のため初データ点がalt=0ではない
   - pad(alt=0) → flight(alt=2) で位置の不連続

### 修正内容
1. **rYOff = drawH/2 * rScale に修正** (app.js)
2. **時間ベース→高度ベース遷移** (app.js)
   - altFrac = min(1, alt/200) → eased = altFrac*(2-altFrac)
   - rScale = padScale + (1-padScale) * eased
   - rYOff = drawH/2 * rScale * (1-eased)
3. **パッドホールド/推力ランプ撤去** (physics.js)
4. **drawH計算をrYOff計算より前に移動** (app.js)
5. **フライトデータにt=0/alt=0の地上点を先頭挿入** (app.js)
   - data[0].alt≈2のギャップを解消、pad→flightで高度が0から連続的に増加
6. **自動ズーム変更を完全廃止→固定ズーム＋ロケット中心追従** (app.js)
   - LIFTOFF時・飛行中ともにtargetZoom変更なし（pad時のH/500を維持）
   - カメラはロケットのワールド座標に常にlerp追従（画面中央に固定）
   - ズーム変更はユーザーのホイール/ピンチ操作のみ
7. **デバッグログ除去** (app.js)

### ブラウザテスト結果
- PADフレーム: nozzle_y == ground_y ✓（ノズルが地面に一致）
- LIFTOFF直後: ジャンプなし、スケール変化なし、ロケット位置維持 ✓
- 飛行中: スムーズなズームアウト、軌跡表示、段分離正常 ✓

### 変更ファイル
- `js/physics.js` — 推力ランプ/パッドクランプ/高解像度記録/liftoffTime撤去
- `js/app.js` — 高度ベース遷移、rYOff修正、drawH計算順序変更、地上点挿入、ダイナミックズーム、デバッグログ除去

---

## 2026-05-29 最適軌道レートリミッター・上段速度ベースガイダンス

### 作業概要
6DOFで最適軌道を描くための飛行フェーズ適応型レートリミッターと、上段の速度ベースガイダンスを実装。

### 実施内容
1. **飛行フェーズ適応型レートリミッター** (physics.js)
   - 固定レートではなく、高度に応じた最適レートでピッチ指令を成形
   - 低高度（<10km）: 1.5°/s — 高Q下でAoA最小化、滑らかなキックオーバー
   - 大気圏中間（10-50km）: 1.5→3.0°/s漸増 — Q低下に合わせ重力ターン追従
   - 上層大気（50-80km）: 3.0°/s — 上段点火への遷移
   - 真空（>80km）: 5.0°/s — 軌道投入に向け積極的ピッチダウン
   - コースト中: prevDesiredPitch=thetaで実姿勢を追従（再点火時のジャンプ防止）

2. **上段ガイダンス: 高度ベース→速度ベースへ** (physics.js)
   - vtRatio = vt/vOrbitから「残り必要水平加速量」を導出
   - 高度成分: (1-altFrac)*20° — 目標高度到達に必要なピッチアップ
   - 速度成分: (1-vtRatio²)*15° — 軌道速度に近づくほど水平に
   - 合成: altPitch*(1-vtRatio) + velPitch（高度未達時は上昇優先、速度不足時は水平加速優先）
   - 真空maxAoA: 20°に復帰（軌道投入に必要な機動性確保）

3. **TVC PD制御ゲイン低減** (physics.js)
   - Kp_tvc: 3.0 → 2.0（追従をソフトに、ジンバル飽和軽減）

4. **1段目ガイダンスのtheta制約除去** (physics.js)
   - `Math.min(theta, ...)` を削除（6DOFではTVCが角度を変えるため不要）

### 変更ファイル
- `js/physics.js` — 適応型レートリミッター、速度ベース上段ガイダンス、Kp低減、theta制約除去

---

## 2026-05-29 L/D比・故障モデル・空力・SSO改修

### 作業概要
L/D≈10比率、フェーズ別故障モデル、Lv1空力無効化、ガレージSVGリアル化、GTO/SSO軌道整理

### 実施内容
1. **L/D≈10ロケット比率** (app.js)
   - rH2=[0,70,110,140], rW2=[0,3.5,5,6] でL/D≈10の細長いロケット
   - 発射塔高さをrH2+20に連動させて塔がロケットより高くなるよう調整

2. **ガレージSVGリアル化** (screens.js)
   - _renderRocketSVGをオジャイブフェアリング+メタリックグラデーション+段間ジョイントに刷新
   - L/D≈10比率でSVG描画、エンジンノズル・排気グロー付き

3. **フェーズ別故障モデル** (physics.js)
   - 旧: 全パーツに対しランダム時間で故障判定
   - 新: フェーズに紐づく故障
     - 段Nエンジン/タンク: 段N燃焼中に燃料消費割合で故障
     - 段間構造: 分離イベント時に分離失敗
     - フェアリング: 分離イベント時に分離失敗
     - OBC: 全飛行中いつでも故障可能（自身のreliabilityBonusは適用外）
   - ペイロードは故障対象から除外

4. **Lv1空力無効化** (physics.js)
   - hasAero = site.level >= 2 フラグ追加
   - dragForce = 0、空力復元/減衰モーメント = 0（Lv1時）

5. **Lv3 GTO廃止・SSO自動傾斜角** (data.js, screens.js, app.js)
   - GTO: minLevel=4追加、Lv3では非表示
   - SSO選択時: G.ssoInclination(altKm) = 96.5+0.004*(alt-200) で自動計算
   - 傾斜角スライダー無効化、「(SSO自動)」ラベル表示
   - 高度変更時にSSO傾斜角を自動更新

### 変更ファイル
- `js/data.js` — GTO minLevel追加、ssoInclination関数
- `js/physics.js` — hasAeroフラグ、フェーズ別故障、条件付き空力
- `js/app.js` — L/D比率、塔スケール、SSO自動傾斜角
- `js/screens.js` — リアルSVGロケット、軌道フィルタ、SSO UI

---

## 2026-05-28 6DOF物理・リアルロケット描画・リフトオフ位置修正

### 作業概要
3DOF→6DOF回転力学へ移行、ロケット描画をリアルイラストに刷新、リフトオフ時の位置ジャンプ修正

### 実施内容
1. **6DOF回転力学** (physics.js)
   - theta(機体角度), omega(角速度), gimbalAngle(ジンバル角) 状態変数追加
   - TVC姿勢制御（PD制御器: Kp=3.0, Kd=1.5, ジンバル上限±5°）
   - 推力方向 = 機体角度 + ジンバル角（thrustDir = theta + gimbalAngle）
   - 角運動力学: TVCトルク、空力復元モーメント（静安定余裕0.08）、空力減衰
   - MOI = 細棒近似（質量減少に伴い縮小）
   - bodyAngleをフライトデータに記録

2. **リアルロケット描画** (app.js)
   - オジャイブフェアリング（ベジェ曲線による流線型ノーズコーン）
   - メタリックグラデーション段ボディ（上段/下段で色差）
   - 段間結合部（やや太いリング状ジョイント）
   - エンジンノズル（台形ベル形状）
   - 排気炎に白色コア追加（2層構造: 外炎+輝度コア）
   - 段分離時: 下段投棄→残段のみ描画（動的高さ計算）
   - フェアリング分離後: 衛星（金色ボディ+青色太陽電池パネル）表示
   - bodyRad（機体角度）で回転（fpaと分離）

3. **リフトオフ位置ジャンプ修正** (app.js)
   - pad→flight遷移時のrScale/rYOffを3秒イーズアウト補間
   - 旧: 瞬間的にpadScale→1, rYOff→0（ジャンプ発生）
   - 新: tFrac=min(1, simTime/3), eased=t*(2-t) で滑らか遷移

### 変更ファイル
- `js/physics.js` — 6DOF状態変数、TVC PD制御、角運動力学、bodyAngle記録
- `js/app.js` — bodyAngle補間、bodyRad変数、リアルロケット描画、位置遷移修正

---

## 2026-05-28 タンク質量再計算・段間構造複数化・墜落爆発・OBC追加

### 作業概要
タンク乾燥質量をアルミ合金密度ベースで再計算（デュアルタンク対応）、段間構造を各段間ごとに設定可能に変更、墜落時の爆発アニメーション追加、OBC（搭載コンピュータ）を新パーツカテゴリとして追加。

### 実施内容
1. **タンク質量再計算** (data.js)
   - LH2/LOXタンクを大幅増量（低密度の液水タンクは大型化が必要）
   - RP-1/LOXは高レアリティで軽量化、CH4/LOXはさらに軽量
   - タンク名にLH2/メタン等の推進剤タイプを反映

2. **段間構造の複数化** (data.js, state.js, physics.js, screens.js)
   - `structure`(単体) → `structures`(配列) に変更
   - 2段式: 1つ、3段式: 2つの段間構造スロット
   - ガレージUIで各段間ごとに個別設定可能（ラベル: "段間構造 (1段-2段)"等）
   - 旧セーブデータの自動マイグレーション対応
   - SVGロケット表示で各段間構造のレアリティ色を反映

3. **墜落爆発アニメーション** (app.js)
   - フライト終了時に高度5km以下かつ失敗の場合、爆発フェーズに遷移
   - 拡大する火球（RadialGradient）+ 30個の破片パーティクル
   - 3秒間の爆発演出後にリザルト画面へ

4. **OBC（搭載コンピュータ）** (data.js, state.js, physics.js, screens.js, gacha.js)
   - 新カテゴリ `obc` に★1〜★5の5パーツ追加
   - `reliabilityBonus`: 全パーツの基礎故障率を低減（最大12%）
   - `guidanceBonus`: 誘導精度向上（将来拡張用）
   - ガレージ・コレクション・ガチャ・パーツ選択UIに統合

### 変更ファイル
- `js/data.js` — CATEGORIES追加、タンク質量更新、OBCパーツ追加、DEFAULT_ROCKET/INVENTORY更新
- `js/state.js` — structures配列・obcフィールド追加、マイグレーション、setRocketPart/getRocketParts/setStageCount更新
- `js/physics.js` — structures配列対応、OBC信頼性ボーナス、構造材Qα限界チェック
- `js/screens.js` — ガレージ段間構造複数スロット、OBCスロット/タブ、SVG構造色、統計計算
- `js/app.js` — 爆発フェーズ（火球+パーティクル）、ロケット非表示制御

---

## 2026-05-28 ロケット設計ゲーム実装

### 作業概要
`rocket_game_concept.md`の設計書に基づき、HTML/CSS/JSでプレイ可能なWebアプリ「ROCKET BUILDER」を実装。

### 実施内容
- ガチャシステム（★1〜★5、排出率・天井システム・ログインボーナス）
- パーツデータベース（エンジン10種、タンク10種、構造材5種、フェアリング5種、ペイロード5種＝計35種）
- ロケット設計画面（パーツ選択、ΔV/T/W/質量のリアルタイム表示、機体SVG）
- 打ち上げシミュレーション（2D軌道力学、重力・抗力・推力、ピッチプログラム自動最適化）
- 打ち上げアニメーション（Canvas描画、リアルタイムテレメトリ表示）
- スコア計算（基礎スコア×軌道倍率×傾斜角倍率×射点倍率×成功ボーナス×軽量ボーナス×衛星倍率）
- 故障確率モデル（基礎故障率+動的故障率、Lv3.5以降で運用限界有効化）
- 射点レベルシステム（Lv1〜Lv5、累計スコアで解放）
- フライトデータグラフ（高度・速度の時系列チャート）
- 記録画面（フライト履歴カード）
- コレクション画面（カテゴリ別タブ、未入手/入手済み表示）
- LocalStorage永続化

### 物理エンジン調整
- ISPスケール3.5倍（単段でLEO到達可能にするゲーム調整）
- 大気共回転を考慮した抗力計算
- 推力/質量流量の整合性を保つ設計（ISPスケールのみで統一）
- ★1パーツでLEO成功率約40〜50%（基礎故障込み）

### 変更ファイル一覧
- `index.html` — メインHTML
- `css/style.css` — 全スタイル（ダークスペーステーマ、縦型モバイルレイアウト）
- `js/data.js` — パーツDB、定数、設定
- `js/state.js` — ゲーム状態管理、LocalStorage
- `js/gacha.js` — ガチャロジック
- `js/physics.js` — 打ち上げシミュレーション物理エンジン
- `js/score.js` — スコア計算
- `js/screens.js` — UI画面レンダリング
- `js/app.js` — メインアプリコントローラ
- `.claude/launch.json` — 開発サーバー設定

## 2026-05-28 多段式ロケット・重力ターン・アニメーション刷新

### 作業概要
ロケットを最大3段式に対応し、重力ターン最適化軌道とサイドビューアニメーションを実装。

### 実施内容

#### 多段式ロケット (1〜3段)
- データモデルを`{stages: [{engine, tank}, ...], structure, fairing, payload, stageCount}`に変更
- `state.js`: `setStageCount()`, ステージ別`setRocketPart()`, `getRocketParts()`を実装
- 旧保存データからの自動マイグレーション
- ガレージUI: 段数セレクタ(1段/2段/3段)、段ラベル(上段/中段/下段)

#### 重力ターン最適化
- kickover高度3kmで初期傾斜2°、以降はゼロ迎角飛行(ピッチ=速度ベクトル方向)
- 大気圏内の最低ピッチ角フロア: `max(0, 5° × (1 - alt/60km))` で低性能ロケットの急降下を防止
- 地面基準の経路角(fpa)と距離表示: `atan2(vr, vt-rotBoost)`

#### アニメーション刷新
- サイドビュー: X=ダウンレンジ, Y=高度、自動スケーリングビューポート
- ロケット機軸を速度ベクトル方向に整列: `ctx.rotate(π/2 - fpaRad)`
- オレンジの軌跡トレイル描画
- ステージ分離エフェクト(黄色の拡張円 + "N段目 点火！")
- ステータス表示: 上昇中→Max-Q通過→上層大気通過中→大気圏離脱

#### 物理エンジン調整
- `GAME_THRUST_SCALE: 2.0`追加(推力2倍、質量流量も比例増加でΔV不変、T/W向上)
- 2段★1パーツ T/W: 1.69、3段★1パーツ T/W: 1.10
- 軌道投入チェック緩和: 高度80%以上 & vt≥vOrbit×0.95 で即座にブレーク
- テレメトリに距離・経路角・段数表示追加

### 変更ファイル一覧
- `js/data.js` — `DEFAULT_ROCKET`, `DEFAULT_INVENTORY`, `MAX_STAGES`定数追加
- `js/state.js` — 多段式データモデル、マイグレーション、`setStageCount()`
- `js/physics.js` — 多段シム、重力ターン、`GAME_THRUST_SCALE`、ダウンレンジ追跡
- `js/screens.js` — 段数セレクタUI、段別パーツスロット、テレメトリ拡張
- `js/app.js` — サイドビューアニメーション、ステージエフェクト、段別パーツ選択
- `css/style.css` — `.stage-count-selector`, `.stage-group`, `.stage-label`スタイル追加

## 2026-05-28 再生速度スライダー・エネルギー最適軌道

### 作業概要
打ち上げアニメーションに再生速度スライダーを追加し、重力ターンパラメータを目標軌道・T/W比に基づく動的計算に変更。

### 実施内容

#### 再生速度スライダー (x1〜x20)
- `screens.js`: `renderLaunchAnimation()`にスピードスライダーHTML追加
- `app.js`: フレームベース→時間ベースアニメーションに全面書き換え
  - `performance.now()`のdtReal×速度倍率による累積simTime方式
  - 速度変更時のタイムジャンプ防止（累積加算方式）
  - `findDataIndex()`でsimTimeに対応するデータ点を検索
- `css/style.css`: `.speed-control`, `.speed-label`スタイル追加（ステータスバー上部に配置）

#### エネルギー最適重力ターン
- `physics.js`: 固定パラメータ→動的計算に変更
  - T/W比: `gameSLThrust / (totalMass0 * g0)`から算出
  - `kickoverAlt`: 1500〜5000m（T/W高→低高度でキック開始）
  - `kickAngle`: 1.4°〜3.5°（高軌道→小さいキック、高T/W→大きいキック）
  - `minPitch`フロア: T/W≥1.8で2°に緩和（高性能機体の最適軌道追従を改善）

### 変更ファイル一覧
- `js/app.js` — 時間ベースアニメーション、速度スライダー対応
- `js/physics.js` — 動的kickoverAlt/kickAngle、T/W依存minPitch
- `js/screens.js` — 速度スライダーHTML追加
- `css/style.css` — `.speed-control`スタイル追加

## 2026-05-28 打ち上げアニメーション刷新・分離シーケンス・地球曲面

### 作業概要
打ち上げアニメーションを全面刷新。地球中心座標系、ステージ分離・フェアリング分離・衛星分離ギミック、カメラ追従、軌道予測表示を実装。

### 実施内容

#### 地球中心座標系 & カメラシステム
- `toWorld(dr, alt)`: ダウンレンジ距離と高度から地球中心座標へ変換（`x = (R+alt)*sin(dr/R)`, `y = (R+alt)*cos(dr/R) - R`）
- 自然な地球曲面表現（低高度ではフラット、高高度ではカーブが見える）
- カメラのlerp追従: `camX += (target - camX) * camLerp`（速度に応じて0.06〜0.15）
- 自動ズーム: `span = max(3000, alt*1.8, dr*0.4)` でビューポート自動調整
- マウスホイール / ピンチジェスチャーでユーザー手動ズーム

#### ステージ分離シーケンス
- `physics.js`: ステージングステートマシン追加（idle → meco_coast → sep_coast → idle）
  - `MECO_COAST = 1.0s`（エンジン停止→分離までの慣性飛行）
  - `SEP_COAST = 1.5s`（分離→次段点火までの慣性飛行）
- `stagingEvents`に`type`フィールド追加: `meco`, `separation`, `ignition`, `fairing`
- `burning`フラグをflightDataに記録（噴射中/慣性飛行を区別）
- アニメーション側: `d.stage`変更で分離デブリ生成 + ステータスメッセージ

#### 分離・フェアリング・衛星ギミック
- 段間分離: デブリパーティクル（分離した段が速度ベクトル垂直方向にドリフト）
- フェアリング分離: 80km以上で2片に分離（左右に展開）
- 衛星分離: 軌道投入成功時にペイロード分離（太陽電池パネル付き衛星形状）
- 分離時は爆発/衝撃波エフェクトなし（故障時と区別）
- ステータスメッセージロック: 分離メッセージが通常ステータスに上書きされない

#### 軌道予測表示
- `computeOrbit()`: vis-viva方程式とケプラー軌道要素から軌道楕円を計算
- 近地点 > 0: 緑の破線、近地点 < 0: 赤の破線
- 高度50km以上 & 速度2000m/s以上で表示

#### 視覚改善
- 地球: グラデーション塗り（明るい緑）、大気グロー拡大、境界線を明るい青に
- ロケット: グロー効果追加（常に視認可能）、炎を大きく鮮やかに
- カメラlerp速度を再生速度に連動

### 変更ファイル一覧
- `js/app.js` — `_playLaunchAnimation()`全面書き換え（地球座標系、カメラ、デブリ、軌道予測）
- `js/physics.js` — ステージングステートマシン、burning フラグ、stagingEvents拡張

## 2026-05-28 アニメーション補間・線形タンジェント操舵法

### 作業概要
打ち上げアニメーションのカクツキを補間で解消し、重力ターンを最適制御理論に基づく線形タンジェント操舵法に置き換え。

### 実施内容

#### アニメーション滑らか化
- `app.js`: `interpData(simTimeCursor)` 線形補間関数を追加
- flightDataの2秒間隔データ点間を連続的に補間（位置・速度・角度すべて）
- テレメトリ表示の丸め処理追加（補間値の小数点制御）
- トレイル追加を距離ベースに変更（補間で常に異なる座標が返るため）

#### 線形タンジェント操舵法（エネルギー最小軌道）
- `physics.js`: 重力ターン（速度ベクトル追従）→ `pitch = atan(C × (1 - alt/pitchEndAlt))` に変更
- 最適制御理論（ポントリャーギンの原理）から導出される操舵則
- `tangentC = (1.2 + targetAlt/500km) × (1 + twFactor×0.3)` — 目標高度・T/W比に応じて動的計算
- `pitchEndAlt = min(targetAlt × 0.7, 250km)` — 水平遷移完了高度
- AoA制約: 大気圏内3°、真空20°（構造的制約と操舵自由度のバランス）
- kickoverAlt上方修正: `2000 + 4000 × (1 - twFactor)` m

#### ピッチプロファイル例（300km軌道、T/W=1.69）
- 25km: 60° | 100km: 46° | 150km: 30° | 200km: 11° | 210km: 0°

### 変更ファイル一覧
- `js/app.js` — interpData補間関数、テレメトリ丸め、トレイル距離チェック
- `js/physics.js` — 線形タンジェント操舵法、tangentC/pitchEndAlt、AoA制約

## 2026-05-28 UI/バランス調整・故障システム刷新

### 作業概要
アニメーションの自動ズーム削除、グロー削除、故障タイミング修正、故障率表示追加、ガチャ確率調整。

### 実施内容

#### カメラ固定スケール
- 自動ズーム（`d.alt * 1.8, d.downrange * 0.4`）を削除
- ユーザーのホイール/ピンチ操作でのみズーム変更可能に
- ロケット中心追従は維持

#### オレンジグロー削除
- ロケット周辺のRadialGradientグローインジケータを完全削除
- ロケット本体とエンジン噴射炎のみ描画

#### 故障タイミング修正
- 基礎故障判定をシミュレーションループ前にプリロールする方式に変更
- 故障発生時刻を0〜600秒の範囲でランダム割当
- ループ内で`t >= scheduledFailTime`チェック→即座にbreak
- 旧方式（ループ後に`failTime = t * Math.random()`で遡及設定）を廃止

#### 故障率更新 & 表示
- baseFail: ★1=30%, ★2=20%, ★3=10%, ★4=5%, ★5=1%
- ガレージ・パーツ選択モーダル・コレクションに故障率を赤字で表示

#### ガチャ確率調整
- ★1: 60%→50%, ★2: 25%→35%（★2の排出率を大幅引き上げ）
- ★3〜★5は据え置き（10%, 4%, 1%）

### 変更ファイル一覧
- `js/data.js` — baseFail率更新、ガチャrate調整
- `js/app.js` — 自動ズーム削除、グローインジケータ削除
- `js/physics.js` — 基礎故障プリロール方式、ループ内故障チェック
- `js/screens.js` — `_partBrief`/`_partDetail`に故障率表示追加

## 2026-05-28 投入精度（高度トレランス）システム

### 作業概要
射点レベルに応じた目標高度の許容誤差を導入。全段燃焼後に高度が許容範囲外ならミッション失敗。

### 実施内容

#### 高度トレランス
- 各射点に`altTolerance`(km)を追加: Lv1=±100, Lv2=±80, Lv3=±50, Lv3.5=±30, Lv4=±20, Lv5=±10
- 全段燃焼完了（`allBurnedOut`フラグ）を検知
- 燃焼後に高度が`targetAlt + tolerance`を超過 → 「目標高度超過」で失敗
- 燃焼後に降下中で高度が`targetAlt - tolerance`未満 → 「目標高度未達」で失敗
- 燃焼後120秒のコーストタイムアウト追加（無限ループ防止）
- 成功判定を`targetAlt ± tolerance`範囲内に更新

#### UI表示
- 打ち上げ画面のサマリに「投入精度: ±Xkm」を表示

### 変更ファイル一覧
- `js/data.js` — SITES に`altTolerance`追加
- `js/physics.js` — 全段燃焼検知、高度トレランスチェック、成功条件更新
- `js/screens.js` — 打ち上げサマリに投入精度表示

## 2026-05-28 射座・アンビリカルマスト・リフトオフボタン・上段姿勢制御

### 作業概要
打ち上げアニメーションに射座シーンとリフトオフボタンを追加。上段ロケットの姿勢制御を水平方向に積極的に向けるよう改善。

### 実施内容

#### 射座・アンビリカルマスト・リフトオフシーケンス
- 3フェーズアニメーション: `pad` → `ignition` → `flight`
- **pad**: ロケットが射座に鎮座、アンビリカルマスト（塔、トラス、サービスアーム、アンビリカルケーブル、避雷針）描画、エンジン停止
- **ignition**: LIFTOFFボタン押下でエンジン点火、炎が徐々に成長(1秒)、排煙エフェクト、サービスアーム切離、2.5秒後にリフトオフ
- **flight**: 既存のフライトアニメーション、カメラがH/500→H/3000に自動ズームアウト
- 射座構造物はワールド座標(0,0)に描画、上昇に伴い自然にフレームアウト
- 速度スライダーはフライトフェーズでのみ表示

#### 上段姿勢制御（水平方向ステアリング）
- 1段目: 既存の線形タンジェント操舵法を維持（保守的、ピッチは単調減少）
- 上段(currentStage > 0): 軌道投入用の積極的な水平ステアリング
  - `schedPitch = max(0, 25° × (1 - altFrac × 1.3))`: 高度に応じて0°へ急速に遷移
  - 真空中AoA上限25°（大気圏内は10°）: 迅速な姿勢変更を許可
  - `pitchAngle`の単調減少制約を解除: 上段は自由にピッチ制御可能

### 変更ファイル一覧
- `js/app.js` — `_playLaunchAnimation()`全面書き換え（3フェーズ統一ループ、射座描画、リフトオフシーケンス）
- `js/physics.js` — 上段ステアリング分離（1段目:タンジェント法、上段:水平積極制御）
- `js/screens.js` — `renderLaunchAnimation()`にLIFTOFFボタン追加
- `css/style.css` — `.liftoff-btn`スタイル（グラデーション、パルスアニメーション）

## 2026-05-29 射座ワールド固定＆ロケットスケール調整

### 作業概要
ズーム操作時のビジュアル改善。射座・アンビリカルマストをワールド座標上で固定サイズ（zoom比例）に変更し、ロケットのスケールにsqrt緩和の上限を設定。

### 実施内容

1. **射座・マストのワールド固定サイズ化**
   - `PAD_BASE_SCALE = 1.2` を基準スケールとして定義
   - `padScale = PAD_BASE_SCALE * (zoom / initialZoom)` でzoomに比例
   - ズームイン→射座拡大、ズームアウト→射座縮小（地球と同じスケール感）

2. **ロケットのスケールキャップ（sqrt緩和）**
   - `maxRocketScale = Math.max(0.4, Math.sqrt(zoom / initialZoom) * 1.2)`
   - sqrt関数で緩やかに縮小（線形より大きめに維持）
   - 最小スケール 0.4 で極端なズームアウト時も視認性を確保

### 変更ファイル一覧
- `js/app.js` — `PAD_BASE_SCALE`/`initialZoom`定義、padScaleをzoom比例に変更、rScaleキャップにsqrt適用

## 2026-05-30 シームレスロケット外観オーバーホール（Phase 2）

### 作業概要
部品間の隙間・青色接続ドットを除去し、実際のロケットのようにシームレスに接続する外観に変更。タンク幅＞ノズル幅の比率修正、段間構造の白色化、隣接部品に応じたタンクドーム自動除去。

### 実施内容
1. **接続インジケータ除去** (garage.js: _renderGrid)
   - 青色接続ドットのレンダリングループを削除
   - `border-color`インラインスタイルを除去

2. **隣接部品検出システム追加** (garage.js: _getAdjacency)
   - 各部品のUID指定で直上・直下の部品カテゴリを返す
   - グリッド位置と幅の一致で隣接判定

3. **_svgEngine() リライト** (garage.js)
   - 推力ベースのノズルサイズ: neHW = 10〜22（10kN〜270kN）
   - 上部にタンク接続時: 幅52のマウントプレート描画（タンクボディと面一）
   - ベルノズルは常にタンクより細い

4. **_svgTank() リライト** (garage.js)
   - ボディ幅を42→52に拡大（x=10, w=52）、ノズルより太く
   - 隣接判定でドーム自動除去: 上に段間/OBC→上ドーム非表示、下にエンジン/段間→下ドーム非表示
   - 容量ベースの推薬充填表示

5. **_svgStructure() リライト** (garage.js)
   - 白色グラデーション（#bbb→#eee→#bbb）に変更
   - エッジをタンクボディ幅に合わせて面一（x=10, w=52, y=0〜36）
   - 分離ボルトマーカー追加

6. **_svgFairing() リライト** (garage.js)
   - ベース幅をタンクボディに合わせ拡大（x=10〜62）
   - 半透明シェルのまま内部ペイロードシルエット表示
   - 底部分離リングをタンク幅に合わせ

7. **_svgOBC() リライト** (garage.js)
   - アダプタコーン底部をタンクボディ幅に合わせ（x=10〜62）
   - 上部リング・下部リングをフラッシュエッジに

8. **キャッシュバスト更新** (index.html)
   - 全JS/CSSの`?v=7`→`?v=8`

### 変更ファイル一覧
- `js/garage.js` — _renderGrid, _getAdjacency追加, _partSVG, _svgEngine/Tank/Structure/Fairing/OBCリライト
- `index.html` — キャッシュバスト v7→v8

## 2026-05-30 外観微調整・重ね配置・ノズルサイズ（Phase 3）

### 作業概要
段間構造・フェアリングの色をタンク色に統一、重ね配置（透過表示）機能追加、エンジンノズルのサイズ調整（推力別ピースワイズ関数）、CubeSat太陽光パネル折りたたみ表示。

### 実施内容
1. **段間構造・フェアリングの色統一** (garage.js)
   - 両方のグラデーションをタンクと同じ `#777→#bbb→#ccc→#bbb→#777` に変更

2. **重ね配置機能** (garage.js)
   - `_canPlace`: `category`パラメータ追加、structure/fairingの場合は重複チェックをスキップ
   - `_isOverlapping(uid)`: 新メソッド、指定パーツが他パーツとセル共有しているか判定
   - `_getAdjacency`: 重なっているstructure/fairingを隣接候補から除外（透過して接続）
   - `_renderGrid`: 重なっている場合は `opacity:0.45; z-index:10` で透過表示
   - ドラッグハンドラ3箇所（move/up/duplicate）にcategory引数を追加

3. **エンジンノズルサイズ調整** (garage.js: _svgEngine)
   - ピースワイズ関数: 10kN→7, 30kN→11, 55kN→26(=タンク幅), 270kN→30
   - チャンバー半幅: `max(5, round(neHW * 0.55))`
   - 55kNでノズル直径がタンク直径と同じ

4. **CubeSat折りたたみパネル** (garage.js: _svgPayload)
   - 展開パネル(幅18)→折りたたみパネル(幅3)に変更
   - 衛星本体側面に密着した状態で表示

5. **キャッシュバスト更新** (index.html)
   - `?v=8`→`?v=9`

### 変更ファイル一覧
- `js/garage.js` — _svgEngine/_svgStructure/_svgFairing/_svgPayload更新、_canPlace/_isOverlapping/_getAdjacency/_renderGrid改修
- `index.html` — キャッシュバスト v8→v9

## 2026-05-30 UX改善・ズーム/パン・パーツ制限・フェアリング形状（Phase 5）

### 作業概要
パーツ1回使用制限、複製ボタン削除、Miro風ズーム/パンワークスペース、エンジンタブアイコンサイズ修正、フェアリング形状分岐（コーン/オジャイブ）。

### 実施内容
1. **パーツ1回使用制限** (garage.js: _renderPalette)
   - インベントリのpartIdカウントから配置済みカウントを減算
   - 残数0のパーツはパレットに非表示
   - 配置/削除時にパレットを再描画

2. **複製ボタン削除** (garage.js)
   - `_duplicatePart()`メソッド削除
   - テンプレートから📋ボタン削除
   - イベントバインディングから`dupClick`削除

3. **Miro風ズーム/パンワークスペース** (garage.js + garage.css)
   - `zoom`, `panX`, `panY`, `panning`ステート追加
   - `_screenToWorkspace()`: スクリーン→ワークスペース座標変換
   - `_applyTransform()`: CSS transform適用（translate+scale）
   - `_onWheel()`: ホイールでカーソル中心ズーム（0.3〜3倍）
   - `_onPointerDown/Move/Up`: 空白ドラッグでパン
   - ドラッグ座標をzoom対応に変換
   - 初期表示時にロケット全体がフィットするよう自動ズーム
   - CSS: `overflow: auto` → `overflow: hidden`, grab/grabbing cursor

4. **エンジンタブアイコン修正** (garage.js: _renderTabs)
   - タブ描画時にカテゴリ先頭パーツのpartDataを渡すよう変更
   - エンジンノズルサイズがタブでも正しく反映

5. **フェアリング形状分岐** (garage.js: _svgFairing)
   - `dragCoefficient >= 0.45`: コーン型（イプシロン風、直線）
   - `dragCoefficient < 0.45`: オジャイブ型（カイロス風、ベジェ曲線）

6. **段間構造の外径連続性修正** (garage.js: _getAdjacency)
   - エッジ隣接に加え、重なり覆い判定をフォールバック追加
   - 段間構造が重なったタンクのドームを自動非表示

### 変更ファイル一覧
- `js/data.js` — PART_GRID.structure.h: 1→2, 構造質量2倍
- `js/garage.js` — パレット/イベント/ズーム/SVG大幅改修
- `css/garage.css` — ワークスペースoverflow/cursor変更
- `index.html` — キャッシュバスト v11→v12

## 2026-05-30 タンクサイズの容積比スケーリング（Phase 6）

### 作業概要
タンクのビジュアルサイズを推薬容量に応じてスケーリング。標準タンク(t1a, 2200)を基準とし、面積比が容積比に一致するよう、長さ方向80%・直径方向20%の比率で変化。

### 実施内容
1. **タンク視覚高さスケーリング** (garage.js: _renderGrid)
   - 容量比 `r = cap / 2200` から高さスケール `sh = pow(r, 0.8)` を計算
   - 視覚セル数 `renderH = max(2, min(8, round(3 * sh)))`
   - ボトムアンカー方式: エンジン接続面を基準に配置（上方向に拡張/縮小）
   - 拡張タンクは `z-index:0` で他パーツの下に描画

2. **タンクSVG動的サイズ** (garage.js: _svgTank)
   - `cellH`パラメータ追加、viewBox高さを `cellH * 36` に動的変更
   - 幅スケール `sw = pow(r, 0.2)` でボディ幅 `bw = max(40, min(64, round(52 * sw)))` を計算
   - ボディX座標を中央寄せ: `bx = round(36 - bw/2)`
   - ドーム/ディテール線をbw基準に動的配置
   - 非標準サイズタンクの上部ドーム強制描画（ボトムアンカーによるギャップ対策）

3. **サイズ一覧**
   - t1a (2200): h=3, bw=52 — 基準
   - t1b (1600): h=2, bw=49
   - t2a (3200): h=4, bw=56
   - t3a (4800): h=6, bw=61
   - t4a (6500): h=7, bw=64
   - t5a (8500): h=8, bw=64

4. **キャッシュバスト更新** (index.html)
   - `?v=12`→`?v=13`

### 変更ファイル一覧
- `js/garage.js` — _renderGrid（タンクrenderH/ボトムアンカー/z-index）、_svgTank（cellH/bw/viewBox動的化/ドームロジック）
- `index.html` — キャッシュバスト v12→v13

## 2026-05-31 射点画面・打ち上げアニメーションへのロケットイラスト反映

### 作業概要
設計したロケットのイラストを打ち上げ射点画面のプレビューと打ち上げ中のCanvasアニメーションの両方に反映。

### 実施内容

1. **射点画面プレビュー改良** (screens.js `_renderRocketPreview`)
   - タンクの容量比スケーリング（`pow(cap/2200, 0.8)`でrenderH計算）をプレビューに適用
   - フェアリング自動延伸（payload+obcセルを覆う）
   - 各パーツの隣接情報（adj）を構築し`_svgTank`/`_svgFairing`にcellHとともに渡す
   - エンジンにはマウントプレート表示用のadj.above情報を渡す

2. **打ち上げアニメーションのデータ駆動化** (app.js `_playLaunchAnimation`)
   - `_stageTanks`/`_stageEngines`配列を構築し、パーツデータに基づく描画
   - **ロケット全体サイズ**: `rH2`/`rW2`を全タンク合計容量比`pow(totalCap/baseCap, 0.35)`でスケーリング
   - **残存割合**: 分離後の高さ計算を容量ベースに変更（`remainFrac = remCap / totalCap`）
   - **ステージ高さ**: 各段の高さを実際のタンク容量比で分配
   - **ステージ幅**: 各段の幅を`pow(tankCap/2200, 0.15)`で計算
   - **フェアリング形状**: `dragCoefficient >= 0.45`でコーン型、`< 0.45`でオジーブ型
   - **エンジンノズル**: 推力`pow(thrust/55, 0.25)`でノズル幅をスケーリング

3. **キャッシュバスト更新** (index.html)
   - `?v=13`→`?v=14`

### データ駆動パラメータ一覧
| パーツ属性 | アニメーション反映先 | スケーリング式 |
|---|---|---|
| tank.propellantCapacity | ステージ高さ・全体サイズ | `pow(cap/baseCap, 0.35)` |
| tank.propellantCapacity | ステージ幅 | `pow(cap/2200, 0.15)` |
| fairing.dragCoefficient | フェアリング形状 | `>= 0.45`: コーン, `< 0.45`: オジーブ |
| engine.vacuumThrust | ノズル幅 | `pow(thrust/55, 0.25)` |

### 変更ファイル一覧
- `js/screens.js` — `_renderRocketPreview`（タンクスケーリング、フェアリング延伸、隣接情報、SVG直接呼び出し）
- `js/app.js` — `_playLaunchAnimation`（rH2/rW2データ駆動、容量ベースremainFrac/stgH、フェアリングcone/ogive、ステージ幅、ノズル推力スケール）
- `index.html` — キャッシュバスト v13→v14

## 2026-05-31 タンクrenderH最小値修正（Phase 7b）

### 作業概要
標準タンク(t1b, 1600)で視覚高さがグリッド高さ(3)より小さい2になるバグを修正。ガレージと射点プレビューの両方で発生。

### 原因
`renderH = Math.max(2, ...)` の最小値クランプが `g.h=3` より小さい2を許可していた。
t1b(cap=1600)のみ `round(3 * pow(1600/2200, 0.8)) = 2` となりrenderH=2。
ボトムアンカー配置と合わせて上部に1セル分の空白が発生し、隣接ロジック(g.h=3)との不整合が生じていた。

### 修正内容
1. **garage.js** (line 224): `Math.max(2, ...)` → `Math.max(g.h, ...)`
2. **screens.js** (line 248): `Math.max(2, ...)` → `Math.max(G.PART_GRID.tank.h, ...)`
3. **index.html**: キャッシュバスト v14→v15

### 変更ファイル一覧
- `js/garage.js` — renderH最小値をg.hに修正
- `js/screens.js` — renderH最小値をG.PART_GRID.tank.hに修正
- `index.html` — キャッシュバスト v14→v15

---

## 2026-07-07 コードレビュー（JS/CSS技術的正当性チェック）

### 作業概要
- js/app.js, garage.js, screens.js, state.js, gacha.js, score.js, css/style.css, css/garage.css をレビュー
- index.html のスクリプト読み込み順・DOM ID・クロスファイル参照を照合
- 発見事項（バグ・リスク・軽微）を親セッションに報告。コード変更なし

### 変更ファイル一覧
- なし（レビューのみ、WORK_LOG.md追記のみ）

---

## 2026-07-07 コードベース技術評価（解析のみ）

### 作業概要
index.html および全JS/CSSを客観的・批判的に技術評価。物理エンジン（physics.js/data.js）は直接精査、UI/アプリロジック（app.js/garage.js/screens.js/state.js/gacha.js/score.js/CSS）はサブエージェントでレビュー。

### 実施内容
- physics.js の運動方程式・誘導則・故障モデル・成功判定を物理的正確性の観点で検証
- 主な指摘: 「6DOF」表記は実際は平面3DOF、段間構造質量が分離時に投棄されない、加速度Gが座標加速度ベース（構造荷重と不一致）、SSO等の逆行軌道でも自転ブーストが常に加算、成功判定breakの早期打ち切り
- アプリ側: 部分成功スコア0点バグ、ガレージ設計の未保存問題、打ち上げアニメのループ残留、イベントリスナー漏れ等を確認

### 変更ファイル一覧
- WORK_LOG.md（本記録のみ、コード変更なし）

---

## 2026-07-08 技術評価で検出した全問題の修正

### 作業概要
前回の技術評価で検出した物理エンジンの誤り・ゲームロジックのバグ・軽微な問題をすべて修正。Nodeヘッドレステスト＋ブラウザ実機検証済み。

### 実施内容（物理エンジン physics.js）
- 「6DOF」誤称をコメント上「平面3DOF」に訂正
- 段間構造の質量を下段分離時に投棄するよう修正（ΔV計算も対応）
- 加速度Gを座標加速度からプロパー加速度（推力+空力の比力）に修正
- 自転ブーストを目標傾斜角に投影（Ω·R·cos i）。SSO等の逆行軌道ではペナルティに
- 大気共回転の高度減衰を廃止（密度減衰が実質の減衰）
- 推力の高度補間を線形から気圧比ベース（T = T_vac − ΔT·e^(−h/H)）に修正
- フェアリング分離で全質量を投棄（旧: 70%のみ）
- Qα判定の無意味なg0乗算を除去
- 軌道判定を軌道要素ベースに刷新: 近地点・遠地点がともに目標帯域内で成功。
  旧実装の「vt≥0.95vOrbitでbreak」が実質エンジンカットオフだったため、
  実機同様の終端誘導（アポジ調整→コースト→高度維持円形化燃焼+スロットル）を新規実装
- dt=0.5→0.25、maxTimeを目標高度でスケール、結果に perigee/apogee を追加
- 検証: 全射点×代表機体×150-2000kmで軌道投入成功（例: lv4 2000km→ペリ1983/アポ2003km）、
  NaNなし、0.6ms/sim

### 実施内容（ゲームロジック）
- score.js: 部分成功が0点になる逆転バグ修正（successBonus=0.25）、軌道帯域の
  整数境界の取りこぼし修正（丸めてから照合）
- garage.js/state.js: 「設計完了」で承認済み設計をG.Stateに永続化。打ち上げは
  常に承認済み設計を使用（編集途中レイアウトの誤使用・リロード後の設計消失を解消）。
  段数上限（3段）チェック追加。ドラッグゴーストのズーム倍率対応。
- app.js: 画面遷移時に打ち上げアニメーションを確実に停止（トークン方式、
  結果画面の二重表示・飛行記録の二重登録を解消）。ガレージ再タップ時の
  documentリスナー蓄積を解消。日次ガチャは抽選成功後に消費。
  打ち上げCanvasのHiDPI対応。旧パーツ選択モーダル系の死にコード削除。
- screens.js: 死にコード削除（旧ガレージUI一式 約200行）、フライトチャートHiDPI対応
- state.js: dailyGachaUsedをデフォルトに追加、ログイン連続日数の重複分岐を統合
- data.js: 「NOVA-9V イオン」→「NOVA-9V エキスパンダー」（Isp380s/200kNはイオンの特性と矛盾）
- style.css: ガチャ結果オーバーレイの全画面クリック領域を追加
- index.html: キャッシュバスト v=21→v=22

### 検証
- node --check 全JSパス、Nodeヘッドレスで物理13シナリオ+モンテカルロ100回
- ブラウザ実機: 初期化→ガレージ承認→打ち上げ完走→記録1件→ガチャ→中断テスト、
  コンソールエラー0件

### 変更ファイル一覧
- js/physics.js（軌道力学・終端誘導・故障判定）
- js/score.js（部分成功スコア・帯域照合）
- js/garage.js（設計承認の永続化・段数上限・ゴースト）
- js/state.js（デフォルト値・承認済み設計API・死にコード）
- js/app.js（アニメ停止トークン・ガレージ破棄・ガチャ順序・HiDPI・死にコード削除）
- js/screens.js（死にコード削除・チャートHiDPI）
- js/data.js（エンジン名）
- css/style.css（ガチャオーバーレイ）
- index.html（v=22）
- WORK_LOG.md（本記録）
