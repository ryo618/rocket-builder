# WORK_LOG

## 2026-05-28 ロケット設計ゲーム実装

### 作業概要
`rocket_game_concept.md`の設計書に基づき、HTML/CSS/JSでプレイ可能なWebアプリ「ROCKET BUILDER」を実装。

### 実施内容
- ガチャシステム（★1〜★5、排出率・天井システム・ログインボーナス）
- パーツデータベース（エンジン10種、タンク10種、構造材5種、フェアリング5種、ペイロード5種＝計35種）
- ロケット設計画面（パーツ選択、ΔV/T/W/質量のリアルタイム表示、機体SVG）
- 打ち上げシミュレーション（2D軌道力学、重力・抗力・推力、ピッチプログラム自動最適化）
- 打ち上げアニメーション（Canvas描画、リアルタイムテレメトリ表示）
- スコア計算（基礎スコア×軌道倍率×傾斜角倍率×射点倍率×成功ボーナス×軽量ボーナス×衛星倍率）
- 故障確率モデル（基礎故障率+動的故障率、Lv3.5以降で運用限界有効化）
- 射点レベルシステム（Lv1〜Lv5、累計スコアで解放）
- フライトデータグラフ（高度・速度の時系列チャート）
- 記録画面（フライト履歴カード）
- コレクション画面（カテゴリ別タブ、未入手/入手済み表示）
- LocalStorage永続化

### 物理エンジン調整
- ISPスケール3.5倍（単段でLEO到達可能にするゲーム調整）
- 大気共回転を考慮した抗力計算
- 推力/質量流量の整合性を保つ設計（ISPスケールのみで統一）
- ★1パーツでLEO成功率約40〜50%（基礎故障込み）

### 変更ファイル一覧
- `index.html` — メインHTML
- `css/style.css` — 全スタイル（ダークスペーステーマ、縦型モバイルレイアウト）
- `js/data.js` — パーツDB、定数、設定
- `js/state.js` — ゲーム状態管理、LocalStorage
- `js/gacha.js` — ガチャロジック
- `js/physics.js` — 打ち上げシミュレーション物理エンジン
- `js/score.js` — スコア計算
- `js/screens.js` — UI画面レンダリング
- `js/app.js` — メインアプリコントローラ
- `.claude/launch.json` — 開発サーバー設定

## 2026-05-28 多段式ロケット・重力ターン・アニメーション刷新

### 作業概要
ロケットを最大3段式に対応し、重力ターン最適化軌道とサイドビューアニメーションを実装。

### 実施内容

#### 多段式ロケット (1〜3段)
- データモデルを`{stages: [{engine, tank}, ...], structure, fairing, payload, stageCount}`に変更
- `state.js`: `setStageCount()`, ステージ別`setRocketPart()`, `getRocketParts()`を実装
- 旧保存データからの自動マイグレーション
- ガレージUI: 段数セレクタ(1段/2段/3段)、段ラベル(上段/中段/下段)

#### 重力ターン最適化
- kickover高度3kmで初期傾斜2°、以降はゼロ迎角飛行(ピッチ=速度ベクトル方向)
- 大気圏内の最低ピッチ角フロア: `max(0, 5° × (1 - alt/60km))` で低性能ロケットの急降下を防止
- 地面基準の経路角(fpa)と距離表示: `atan2(vr, vt-rotBoost)`

#### アニメーション刷新
- サイドビュー: X=ダウンレンジ, Y=高度、自動スケーリングビューポート
- ロケット機軸を速度ベクトル方向に整列: `ctx.rotate(π/2 - fpaRad)`
- オレンジの軌跡トレイル描画
- ステージ分離エフェクト(黄色の拡張円 + "N段目 点火！")
- ステータス表示: 上昇中→Max-Q通過→上層大気通過中→大気圏離脱

#### 物理エンジン調整
- `GAME_THRUST_SCALE: 2.0`追加(推力2倍、質量流量も比例増加でΔV不変、T/W向上)
- 2段★1パーツ T/W: 1.69、3段★1パーツ T/W: 1.10
- 軌道投入チェック緩和: 高度80%以上 & vt≥vOrbit×0.95 で即座にブレーク
- テレメトリに距離・経路角・段数表示追加

### 変更ファイル一覧
- `js/data.js` — `DEFAULT_ROCKET`, `DEFAULT_INVENTORY`, `MAX_STAGES`定数追加
- `js/state.js` — 多段式データモデル、マイグレーション、`setStageCount()`
- `js/physics.js` — 多段シム、重力ターン、`GAME_THRUST_SCALE`、ダウンレンジ追跡
- `js/screens.js` — 段数セレクタUI、段別パーツスロット、テレメトリ拡張
- `js/app.js` — サイドビューアニメーション、ステージエフェクト、段別パーツ選択
- `css/style.css` — `.stage-count-selector`, `.stage-group`, `.stage-label`スタイル追加

## 2026-05-28 再生速度スライダー・エネルギー最適軌道

### 作業概要
打ち上げアニメーションに再生速度スライダーを追加し、重力ターンパラメータを目標軌道・T/W比に基づく動的計算に変更。

### 実施内容

#### 再生速度スライダー (x1〜x20)
- `screens.js`: `renderLaunchAnimation()`にスピードスライダーHTML追加
- `app.js`: フレームベース→時間ベースアニメーションに全面書き換え
  - `performance.now()`のdtReal×速度倍率による累積simTime方式
  - 速度変更時のタイムジャンプ防止（累積加算方式）
  - `findDataIndex()`でsimTimeに対応するデータ点を検索
- `css/style.css`: `.speed-control`, `.speed-label`スタイル追加（ステータスバー上部に配置）

#### エネルギー最適重力ターン
- `physics.js`: 固定パラメータ→動的計算に変更
  - T/W比: `gameSLThrust / (totalMass0 * g0)`から算出
  - `kickoverAlt`: 1500〜5000m（T/W高→低高度でキック開始）
  - `kickAngle`: 1.4°〜3.5°（高軌道→小さいキック、高T/W→大きいキック）
  - `minPitch`フロア: T/W≥1.8で2°に緩和（高性能機体の最適軌道追従を改善）

### 変更ファイル一覧
- `js/app.js` — 時間ベースアニメーション、速度スライダー対応
- `js/physics.js` — 動的kickoverAlt/kickAngle、T/W依存minPitch
- `js/screens.js` — 速度スライダーHTML追加
- `css/style.css` — `.speed-control`スタイル追加

## 2026-05-28 打ち上げアニメーション刷新・分離シーケンス・地球曲面

### 作業概要
打ち上げアニメーションを全面刷新。地球中心座標系、ステージ分離・フェアリング分離・衛星分離ギミック、カメラ追従、軌道予測表示を実装。

### 実施内容

#### 地球中心座標系 & カメラシステム
- `toWorld(dr, alt)`: ダウンレンジ距離と高度から地球中心座標へ変換（`x = (R+alt)*sin(dr/R)`, `y = (R+alt)*cos(dr/R) - R`）
- 自然な地球曲面表現（低高度ではフラット、高高度ではカーブが見える）
- カメラのlerp追従: `camX += (target - camX) * camLerp`（速度に応じて0.06〜0.15）
- 自動ズーム: `span = max(3000, alt*1.8, dr*0.4)` でビューポート自動調整
- マウスホイール / ピンチジェスチャーでユーザー手動ズーム

#### ステージ分離シーケンス
- `physics.js`: ステージングステートマシン追加（idle → meco_coast → sep_coast → idle）
  - `MECO_COAST = 1.0s`（エンジン停止→分離までの慣性飛行）
  - `SEP_COAST = 1.5s`（分離→次段点火までの慣性飛行）
- `stagingEvents`に`type`フィールド追加: `meco`, `separation`, `ignition`, `fairing`
- `burning`フラグをflightDataに記録（噴射中/慣性飛行を区別）
- アニメーション側: `d.stage`変更で分離デブリ生成 + ステータスメッセージ

#### 分離・フェアリング・衛星ギミック
- 段間分離: デブリパーティクル（分離した段が速度ベクトル垂直方向にドリフト）
- フェアリング分離: 80km以上で2片に分離（左右に展開）
- 衛星分離: 軌道投入成功時にペイロード分離（太陽電池パネル付き衛星形状）
- 分離時は爆発/衝撃波エフェクトなし（故障時と区別）
- ステータスメッセージロック: 分離メッセージが通常ステータスに上書きされない

#### 軌道予測表示
- `computeOrbit()`: vis-viva方程式とケプラー軌道要素から軌道楕円を計算
- 近地点 > 0: 緑の破線、近地点 < 0: 赤の破線
- 高度50km以上 & 速度2000m/s以上で表示

#### 視覚改善
- 地球: グラデーション塗り（明るい緑）、大気グロー拡大、境界線を明るい青に
- ロケット: グロー効果追加（常に視認可能）、炎を大きく鮮やかに
- カメラlerp速度を再生速度に連動

### 変更ファイル一覧
- `js/app.js` — `_playLaunchAnimation()`全面書き換え（地球座標系、カメラ、デブリ、軌道予測）
- `js/physics.js` — ステージングステートマシン、burning フラグ、stagingEvents拡張
